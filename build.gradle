apply plugin: 'eclipse'
apply plugin: 'java'

ext.helloWorldValue = System.env.HELLO_WORLD ?: 'world'

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.12'
}

task helloWorld {
    doLast {
        println "Hello $helloWorldValue!"
    }
}
